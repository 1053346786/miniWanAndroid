const host = 'https://www.wanandroid.com/';

const config = {
  // 轮播图地址
  bannerUrl: host + 'banner/json',
  homeListUrl: host + 'article/list/',
  systemListUrl: host + 'tree/json',

  loginUrl: host + '/user/login',
  register: host + '/user/register'
}
module.exports = config
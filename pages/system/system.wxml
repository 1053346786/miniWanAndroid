<view wx:for="{{systemListData}}" wx:key="{{item.id}}">
  <text class='item1'>{{item.name}}</text>

  <view class='rowContainer'>
    <view wx:for="{{item.children}}" wx:key="{{item.children.id}}">
      <text class='item2'>{{item.name}}</text>
    </view>
  </view>

</view>